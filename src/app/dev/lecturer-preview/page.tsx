import LecturerPreview from '@/components/dashboard/lecturer/LecturerPreview'

export default async function LecturerPreviewPage() {
  // This page is ONLY for UI preview (no DB calls here).
  return <LecturerPreview />
}